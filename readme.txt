To get things to run, do the follwing:

Pre:
    Have Node.js installed

1. npm install --save express
2. npm install ejs --save
3. npm install mysql --save
4. pm install body-parser --save


Start server:

    node .\server.js 